# Play Spotify over Sonos using NFC tags

## Dependencies

```
sudo apt-get install libpcsclite1 libpcsclite-dev
sudo apt-get install pcscd
```

## Getting started

Add the IP of your Sonos player to `settings.json`

```
npm i
npm start
```
